﻿@if (reservationDTO is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <ReservationForm @ref="reservationForm" ReservationDTO="reservationDTO" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}